# 本文件是本地敏感配置示例，请复制为 application-local.yml 后再填写真实值。
# application-local.yml 已加入 .gitignore，不要提交真实数据库密码。
spring:
  datasource:
    password: 本机数据库密码

app:
  security:
    # 生产环境必须替换为真实前端域名，多个来源用英文逗号分隔。
    cors:
      allowed-origins: http://localhost:5173,http://127.0.0.1:5173
