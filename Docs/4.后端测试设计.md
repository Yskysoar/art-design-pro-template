# 后端测试设计

最后更新：2026-06-01

## 一、测试目标

后端测试需要保证当前模板后端在继续扩展业务模块时不会破坏已有认证、权限、菜单、配置项和通用响应契约。

优先目标：

- Service 层业务规则可独立验证，不依赖真实 MySQL。
- Controller 层接口契约可验证，包括认证、参数校验、响应结构和状态码。
- 数据库脚本可验证，避免 schema、mock SQL 和实体字段不一致。
- 前端已接入的真实接口在后端重启后可以快速回归。

## 二、测试分层

| 层级 | 工具 | 是否依赖数据库 | 主要验证 |
| :--- | :--- | :--- | :--- |
| 单元测试 | JUnit 5 + Mockito + AssertJ | 否 | Service 业务规则、边界条件、异常分支 |
| Web 层测试 | Spring Boot Test + MockMvc + spring-security-test | 否或弱依赖 | Controller 参数校验、认证拦截、统一响应 |
| 数据库集成测试 | Testcontainers MySQL 或本地测试库 | 是 | MyBatis-Plus 映射、SQL 脚本、分页、唯一约束 |
| HTTP 回归测试 | PowerShell/IDE HTTP Client | 是，依赖运行中后端 | 登录、菜单、用户、角色、配置项真实接口 |

当前已落地第一层 Service 单元测试，并开始补充 Web 层 MockMvc 权限入口测试。

2026-06-01 已完成接口权限、配置项规则和用户组织关系非法配置单元测试回归：新增权限服务后，系统管理主要接口已接入权限码入口校验；配置项已覆盖非法配置值、禁止修改配置键、禁止删除系统内置配置；用户组织关系已覆盖非法配置值拒绝；权限服务已覆盖超级管理员必须同时存在于 JWT 声明和数据库启用角色数据中；权限不足的业务异常已映射为 HTTP 403。用户、角色、菜单、组织、配置项 Controller 已覆盖无 token 401、无权限 403、有权限 200 及主要写接口权限校验 MockMvc 用例。

2026-06-01 已完成配置项真实接口回归：健康检查、登录、配置项列表、新增、重复配置键校验、更新、删除后不可见均通过；前端 Vite 代理访问登录、菜单和配置项列表也通过。

2026-06-01 已完成组织管理真实接口回归：组织树、新增、更新、删除后不可见均通过；前端 Vite 代理访问组织树通过。组织删除同样使用 MyBatis-Plus `deleteById` 接入逻辑删除。

2026-06-01 已完成角色数据权限真实接口回归：`GET /api/role/{id}/data-scope`、`PUT /api/role/{id}/data-scope` 通过；`CUSTOM_ORG` 可保存组织范围，`SELF` 会清空自定义组织关系，空组织的 `CUSTOM_ORG` 会返回业务错误；前端角色页已接入数据权限弹窗和前端代理访问。

2026-06-01 已完成组织树数据权限过滤单元测试：超级管理员返回全量组织树；普通管理员按 `CURRENT_ORG` 返回当前组织，按 `CURRENT_ORG_AND_SUB` 返回当前组织及下级，按 `CUSTOM_ORG` 返回自定义组织及下级，`SELF` 在组织树场景按当前组织处理；没有可见组织范围时返回空树；角色数据范围脏值返回业务错误。执行 `mvn test` 结果：`Tests run: 52, Failures: 0, Errors: 0, Skipped: 0`。

2026-06-01 已修复 JWT 解析异常边界：无效或过期 Bearer token 在过滤器层统一返回 HTTP 401 和统一 JSON 响应，不再进入全局异常兜底。执行 `mvn test` 结果：`Tests run: 53, Failures: 0, Errors: 0, Skipped: 0`。

2026-06-01 已完成真实角色账号权限联调：先按项目 SQL 重置 `backend_template` 数据库，再验证 `admin/admin123` 登录后拥有系统管理菜单、`system:*` 按钮和系统管理接口 200；`moderator/admin123` 登录后仅拥有文章菜单和 `article:*` 按钮，访问用户、角色、菜单管理、组织、配置项接口均返回 HTTP 403。测试完成后已再次执行 `db/schema` 与 `db/mock` SQL，恢复标准开发数据。

## 三、配置项模块测试用例

### 3.1 Service 单元测试

测试文件：

```text
Code/backend-template-java/src/test/java/com/template/system/config/service/impl/ConfigServiceImplTest.java
```

已覆盖：

| 用例 | 目的 |
| :--- | :--- |
| 分页查询修正非法分页参数 | `current < 1` 回到 1，`size` 最大限制为 100 |
| 分页查询字段转换 | `editable`、时间格式、列表结构正确 |
| 新增配置项成功 | 配置键唯一时写入配置值、可编辑标记、创建人和更新人 |
| 新增配置项重复 | 重复 `configKey` 抛出业务异常，不执行插入 |
| 新增内置配置项非法值 | 内置配置键只允许约定枚举或布尔值 |
| 更新可编辑配置项 | 可编辑配置允许修改业务字段和更新人 |
| 更新只读配置项 | `editable=0` 时拒绝修改 |
| 更新配置键 | 后端拒绝修改已有配置项的 `configKey` |
| 删除可编辑配置项 | 执行逻辑删除，不物理删除 |
| 删除系统内置配置项 | 系统内置配置不允许删除 |
| 删除不存在配置项 | 不存在时抛出业务异常 |

说明：配置项删除使用 MyBatis-Plus `deleteById`。在当前全局逻辑删除配置下，该方法会执行逻辑删除更新，并让后续查询自动排除 `deleted=1` 数据。

### 3.2 Controller 权限入口测试

测试文件：

```text
Code/backend-template-java/src/test/java/com/template/system/config/controller/ConfigControllerTest.java
```

已覆盖：

| 接口 | 场景 |
| :--- | :--- |
| `GET /api/config/list` | 未登录返回 401；无权限返回 403；有权限返回分页数据 |
| `POST /api/config` | 有权限返回 200；无权限返回 403 |
| `PUT /api/config/{id}` | 有权限返回 200，并进入 Controller 写接口流程 |
| `DELETE /api/config/{id}` | 有权限返回 200，并进入 Controller 写接口流程 |

## 四、用户管理模块测试用例

### 4.1 Controller 权限入口测试

测试文件：

```text
Code/backend-template-java/src/test/java/com/template/system/user/controller/UserControllerTest.java
```

已覆盖：

| 用例 | 目的 |
| :--- | :--- |
| 无 token 访问用户列表 | 返回 HTTP 401 和统一 JSON 响应 |
| 无效 token 访问用户列表 | 返回 HTTP 401 和统一 JSON 响应 |
| 无权限角色访问用户列表 | 返回 HTTP 403 和权限不足消息 |
| 有权限角色访问用户列表 | 返回 HTTP 200 和分页数据 |

## 五、系统管理 Controller 权限入口测试

测试辅助文件：

```text
Code/backend-template-java/src/test/java/com/template/support/web/ControllerSecurityTestSupport.java
```

该辅助类统一生成测试 JWT，并通过 Mock `PermissionService` 验证无权限时的 403 响应。

已落地测试文件：

| 模块 | 测试文件 | 已覆盖重点 |
| :--- | :--- | :--- |
| 角色 | `Code/backend-template-java/src/test/java/com/template/system/role/controller/RoleControllerTest.java` | 列表 401/403/200；新增、编辑、删除、菜单授权、数据权限接口权限校验 |
| 菜单 | `Code/backend-template-java/src/test/java/com/template/system/menu/controller/MenuControllerTest.java` | 管理树 401/403/200；新增、编辑、删除接口权限校验 |
| 组织 | `Code/backend-template-java/src/test/java/com/template/system/org/controller/OrgControllerTest.java` | 组织树 401/403/200；新增、编辑、删除接口权限校验 |
| 配置项 | `Code/backend-template-java/src/test/java/com/template/system/config/controller/ConfigControllerTest.java` | 列表 401/403/200；新增、编辑、删除接口权限校验 |

已验证命令：

```powershell
D:\Coding\Maven\apache-maven-3.9.15\bin\mvn.cmd test
```

结果：`Tests run: 53, Failures: 0, Errors: 0, Skipped: 0`。

## 六、系统管理模块测试规划

| 模块 | 单元测试重点 | 集成测试重点 |
| :--- | :--- | :--- |
| 认证 | 密码校验、JWT 生成、禁用用户拒绝登录 | 登录成功/失败、401 响应 |
| 用户 | 用户名唯一、默认密码、角色关系重写、角色层级约束、逻辑删除 | 分页查询、创建用户、启停用 |
| 角色 | 角色编码唯一、系统角色保护、角色层级约束、已绑定用户禁止删除 | 授权菜单、保存权限 |
| 角色数据权限 | 数据范围枚举、CUSTOM_ORG 校验、自定义组织清空 | 查询保存数据权限、组织范围回写 |
| 菜单 | 父级校验、防循环、按钮权限挂载、删除保护 | 后端路由树结构、按钮 `authList` |
| 组织 | 祖级链维护、子节点/关联保护 | 组织树返回、删除约束 |
| 配置项 | 唯一键、只读保护、逻辑删除 | 分页、创建、更新、删除 |

### 6.1 组织树数据权限测试

测试文件：

```text
Code/backend-template-java/src/test/java/com/template/system/org/service/impl/OrgServiceImplTest.java
```

已覆盖：

| 用例 | 验证点 |
| :--- | :--- |
| 超级管理员查询组织树 | `R_SUPER` 且数据库角色启用时返回全量组织树 |
| 当前组织 | `CURRENT_ORG` 返回当前用户组织和必要祖先节点，不返回下级组织 |
| 当前组织及下级 | `CURRENT_ORG_AND_SUB` 返回当前用户组织、下级组织和必要祖先节点 |
| 本人数据 | `SELF` 在组织树场景按当前用户组织处理 |
| 自定义组织 | `CUSTOM_ORG` 返回角色自定义组织、下级组织和必要祖先节点 |
| 无可见组织 | 当前用户没有组织范围时返回空组织树 |
| 非法数据范围 | 角色数据范围脏值返回业务错误，避免接口变成 500 |

## 七、运行命令

后端单元测试：

```powershell
D:\Coding\Maven\apache-maven-3.9.15\bin\mvn.cmd test
```

含义：使用本机 Maven 执行后端全部测试，不需要启动后端服务。

仅运行配置项测试：

```powershell
D:\Coding\Maven\apache-maven-3.9.15\bin\mvn.cmd -Dtest=ConfigServiceImplTest test
```

含义：只执行配置项 Service 单元测试，适合快速回归当前模块。

## 八、真实接口回归建议

Agent 可自行启动后端后，按以下顺序做只读回归：

```powershell
$login = Invoke-RestMethod -Uri http://localhost:8080/api/auth/login -Method Post -ContentType 'application/json' -Body '{"userName":"admin","password":"admin123"}'
Invoke-RestMethod -Uri 'http://localhost:8080/api/config/list?current=1&size=20' -Method Get -Headers @{ Authorization = "Bearer $($login.data.token)" }
```

含义：先登录获取 JWT，再读取配置项分页列表。该命令不写数据库。

写接口回归建议先使用临时配置键，例如：

```text
dev_test_config_当前日期
```

测试后立即删除，避免污染长期 Mock 数据。

## 九、数据库测试规则

必要时 Agent 可以执行 SQL 文件或连接数据库做测试，但必须遵守以下边界：

- 只能操作本项目的 `backend-template` 数据库。
- 禁止对其他数据库执行写入、删除、重建、初始化或测试操作。
- 可以在 `backend-template` 数据库中进行接口联调、写接口回归和 SQL 验证。
- 数据库测试完成后必须重新初始化 `backend-template` 数据库。
- 重新初始化应执行 `Code/backend-template-java/src/main/resources/db/` 下的结构 SQL 和 Mock SQL，恢复标准开发数据。
- 数据库密码只允许来自环境变量或本机未追踪配置，不得写入 Git 跟踪文件、Docs 或提交信息。

## 十、本轮测试记录：用户组织关联

更新时间：2026-06-01

本轮新增用户组织关联测试，覆盖以下场景：

| 用例 | 验证点 |
| :--- | :--- |
| 用户分页返回组织 ID | `/api/user/list` 返回 `orgIds`，前端用户编辑弹窗可回填组织树 |
| 新增用户写入组织关系 | 新增用户时同步写入 `sys_user_role` 和 `sys_user_org` |
| 一对一组织模式校验 | `user_org_relation_mode=ONE_TO_ONE` 时，后端拒绝一个用户关联多个组织 |
| 组织存在性校验 | 保存不存在的组织 ID 时返回业务错误，避免产生脏关系 |

已落地测试文件：

```text
Code/backend-template-java/src/test/java/com/template/system/user/service/impl/UserServiceImplTest.java
```

已验证命令：

```powershell
D:\Coding\Maven\apache-maven-3.9.15\bin\mvn.cmd test
```

结果：`Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`。
